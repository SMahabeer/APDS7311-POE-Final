// src/Home.js
import React from 'react';
import Buttons from './Components/Buttons/Buttons'; // Import the Buttons component

const Home = () => {
  return (
    <div>
      <Buttons /> {/* Render the Buttons component here */}
    </div>
  );
};

export default Home;
